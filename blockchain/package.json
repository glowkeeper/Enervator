{
  "name": "Enervator",
  "version": "0.0.1",
  "description": "A cryptocurrency stablecoin, EOR, whose value is linked to energy",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/glowkeeper/Enervator.git"
  },
  "scripts": {
    "generate": "mkdir -p ./build/contracts && touch ./build/contracts/fooBar.json && rm ./build/contracts/*json && truffle compile --all && ./typechainBuild.sh",
    "develMigrate": "truffle migrate --network development",
    "rinkebyMigrate": "truffle migrate --network rinkeby",
    "ropstenMigrate": "truffle migrate --network ropsten",
    "test": "truffle test"
  },
  "keywords": [],
  "author": "Steve Huckle",
  "license": "LGPL-3.0",
  "dependencies": {
    "@openzeppelin/contracts": "^2.3.0",
    "bignumber.js": "^9.0.0",
    "bn.js": "^5.0.0",
    "decimal.js": "^10.2.0",
    "ethers": "^4.0.37"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-bn": "^0.2.0",
    "openzeppelin-test-helpers": "^0.4.3",
    "typechain": "^0.3.21"
  }
}
